<% if is_logged_in %>
  <div class="row">
    <% @users.each do |user| %>

    <div class="card" style="width: 18rem;">
      <div class="card-body">
        <h5 class="card-title"><%= user.first_name %></h5>
        <h5 class="card-title"><%= user.last_name %></h5>

        <h6 class="card-subtitle mb-2 text-muted"><%= user.email %></h6>
      </div>
    </div>
    <% end %>
  </div>
<% else %>
  <div class="jumbotron">
  <h1 class="display-4">Bien essayé!!!</h1>
  <p class="lead">Pour acceder a la page Club, inscris toi ou Log toi, c'est gratuit et rapide ! </p>
  <hr class="my-4">
  <p class="lead">
    <a class="btn btn-primary btn-lg"<%= link_to 'Login' , login_path %></a>
    <a class="btn btn-primary btn-lg" <%= link_to 'Inscription', new_user_path %> </a>

  </p>
</div>
<% end %>
